/**
 * WordPress Libraries
 */
const { withSelect } = wp.data;

/**
 * Wrapper Component
 */
export const Wrapper = props => {
	return (
		<div className="reacts-block-wrap">
		</div>
	);
};

/**
 * SmartWrapper Component
 * This wrapper is aware of the state
 */
export const SmartWrapper = false;
